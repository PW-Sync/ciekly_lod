import { Button, VerticalBox } from "std-widgets.slint";

export component App inherits Window {
    title: @tr("Ciekly Lod | PW Sync GUI");
    icon: @image-url("res/fav.png");

    VerticalBox {
        Text { text: "hello world."; }
        Button { text: "wow"; }
    }
}
